name: Hello World

on:
  push:
    branches:
      - version-2.0
  pull_request:
    branches:
      - main

jobs:
  say-hello:
    runs-on: ubuntu-latest # The "runner" (virtual machine) to use

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Print message
        run: echo "Hello, GitHub Actions!"
